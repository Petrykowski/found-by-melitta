import styled from 'styled-components';
import { media } from './../../mediaStyles.js';

const StyledOrderHeader = styled.img`
  
  ${media.desktop`

  `};
};
`;

StyledOrderHeader.defaultProps = {
  theme: {
  },
};

export default StyledOrderHeader;
